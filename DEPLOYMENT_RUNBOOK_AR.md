# دليل النشر الآمن لمنصة بكلمة

هذا الدليل هو بوابة النشر العملية. لا يُنشر أي إصدار إذا فشل أحد فحوص البناء، ولا تُطبّق تغييرات قاعدة البيانات على الإنتاج قبل نسخة احتياطية قابلة للاستعادة.

## 1. البيئات

- **التطوير المحلي:** قاعدة بيانات وحسابات دفع تجريبية مستقلة.
- **Preview / Staging:** نشر تلقائي لكل Pull Request مع قاعدة بيانات تجريبية منفصلة تماماً عن الإنتاج.
- **Production:** نطاق وقاعدة بيانات ومفاتيح Stripe الحية وحدها. لا تُنسخ أسرار الإنتاج إلى Preview.

المتغيرات المطلوبة حسب الميزات المفعلة:

- `DATABASE_URL`, `PUBLIC_APP_URL`, `CRON_SECRET`
- `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`
- `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM`
- مفاتيح Google وWhatsApp الموجودة سابقاً عند تفعيل خدمتيهما

يتحقق الخادم عند الإقلاع من المتغيرات الإلزامية في الإنتاج، ومن اكتمال كل تكامل اختياري: لا يمكن مثلاً ضبط مفتاح Stripe دون سر Webhook، أو معرّف Google دون سره. يجب أن تستخدم روابط الإنتاج بروتوكول HTTPS.

يجب أن تكون قيمة `PUBLIC_APP_URL` رابط الواجهة العامة، وأن يكون Webhook في Stripe موجهاً إلى `/api/webhooks/stripe` في البيئة نفسها.

## 2. بوابة ما قبل النشر

من جذر المستودع:

```bash
pnpm install --frozen-lockfile
pnpm check:release
```

يشغّل GitHub Actions الأمرين تلقائياً في كل Pull Request وعلى الفرع `main`. اختبارات E2E تحتاج خادماً وقاعدة بيانات اختبار، لذلك تُشغّل قبل الإنتاج في بيئة Staging:

```bash
DATABASE_URL="postgresql://...staging..." E2E_BASE_URL="https://staging.example.com" pnpm test
```

## 3. قاعدة البيانات والترحيلات

1. خذ نسخة احتياطية مشفرة من قاعدة الإنتاج وجرّب استعادتها دورياً في قاعدة معزولة.
2. طبّق الترحيل أولاً على Staging واختبر التسجيل والدفع والإلغاء والاسترداد.
3. طبّق ملفات `lib/db/drizzle/*.sql` الجديدة بالترتيب عبر أمر الترحيل المتتبع. الترحيل الحالي هو `0011_activity_self_assessment.sql`، وهو يضيف عمودَي التقييم الذاتي إلى `activity_submissions`.
4. لا تستخدم `push-force` على Production.
5. بعد الترحيل، افحص الجداول والفهارس ثم انشر التطبيق. الترحيل الحالي إضافي ولا يحذف بيانات قديمة.

أمر التنفيذ بعد أخذ النسخة الاحتياطية:

```bash
pnpm --filter @workspace/db migrate
```

## 4. فحص ما بعد النشر

- افتح الصفحة الرئيسية من هاتف بعرض 320px ومن سطح المكتب.
- أنشئ طلب تسجيل وجاهي تجريبي، ثم عدله وألغِه من رابط الإدارة.
- طبّق كود خصم تجريبي، وأكمل دفعة Stripe في الوضع التجريبي، وتأكد من ظهور سجل أحداث الطلب.
- أعد إرسال Webhook نفسه وتأكد أنه لا ينشئ تسجيلاً أو خصماً ثانياً.
- افحص لوحة الإدارة: الدورات الوجاهية، الطلبات، الخصومات، والتحليلات.
- نفّذ `/api/healthz` وتحقق من أخطاء الخادم والسجلات.

## 5. المهام الدورية

استدعِ المسارين التاليين يومياً من خدمة Cron موثوقة مع `Authorization: Bearer CRON_SECRET`:

- `POST /api/cron/in-person-reminders`
- `POST /api/cron/speech-retention`

## 6. التراجع عند المشكلة

- أوقف استقبال الزيارات إلى الإصدار الجديد أو أعد توجيهها إلى آخر إصدار سليم.
- لا تتراجع عن ترحيل قاعدة البيانات عشوائياً. التغييرات الحالية إضافية، لذا يمكن للإصدار السابق تجاهل الجداول والأعمدة الجديدة.
- إذا حصل فساد بيانات، أوقف الكتابة واستعد آخر نسخة سليمة ثم أعد Webhooks الموثقة فقط.
- احتفظ بمعرّف الإصدار، وقت النشر، منفّذ النشر، ونتيجة فحص ما بعد النشر في سجل الإصدارات.
