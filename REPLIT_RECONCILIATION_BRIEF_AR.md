# تعليمات دمج نسخة Replit الأحدث مع تعديلات Codex

انسخ النص التالي كاملاً إلى وكيل Replit. الهدف هو الحفاظ على النسخة الأحدث الموجودة في Replit، وإدخال التحسينات المطلوبة من فرع Codex بصورة انتقائية وآمنة، ثم رفع النتيجة إلى GitHub.

---

## المهمة

لدي نسختان من مشروع «بكلمة»:

1. النسخة الموجودة حالياً في مساحة عمل Replit، وهي النسخة الأحدث وظيفياً، ويجب اعتبارها **الأساس ومصدر الحقيقة**.
2. فرع GitHub باسم `codex/launch-wave`، وفيه تعديلات نفذها Codex فوق أساس أقدم. آخر commit فيه هو:
   `65c3c393c31e993a814652f7b3814fe7041822db`

المطلوب هو دمج **السلوكيات والتحسينات الجديدة فقط** من فرع Codex داخل نسخة Replit الحالية، من دون استبدال المشروع كله، ومن دون فقد أي تحسين أحدث موجود في Replit.

## قواعد إلزامية قبل التنفيذ

- اعتبر نسخة Replit الحالية هي الـbase. لا تجعل `main` القديم أو `codex/launch-wave` أساساً جديداً للمشروع.
- لا تنفذ merge أو cherry-pick للفرع كله، ولا تنسخ مجلدات أو ملفات كاملة فوق نسخة Replit تلقائياً.
- أنشئ أولاً فرع نسخ احتياطي مؤرخ للنسخة الحالية في Replit، واحفظ فيه كل التغييرات الحالية في commit واضح، ثم أنشئ منه فرع عمل جديد باسم قريب من `replit/reconcile-codex-launch-wave`.
- اجلب `origin/codex/launch-wave` للمقارنة فقط، ثم نفذ دمجاً دلالياً feature-by-feature.
- قبل تعديل كل بند، افحص هل هو: **منجز في Replit / موجود جزئياً / جديد فعلاً / متعارض**. أخرج مصفوفة تقاطع بهذه الحالات قبل كتابة الكود.
- إذا وجدت في Replit تنفيذاً أحدث أو أجمل أو أكثر اكتمالاً للميزة نفسها، احتفظ ببنيته وتصميمه، وأضف إليه المتطلب الناقص فقط.
- لا تعِد بناء أي ميزة قائمة، ولا تستبدل CRM أو المصادقة أو لوحات المستخدمين لمجرد أن ملفات فرع Codex أقدم.
- لا تحذف أي شيء غير مصرح به. الحذف المصرح به صراحة هو:
  1. فقرة/مكوّن الاختبار السريع «ما البرنامج المناسب لك؟».
  2. أي تحليل آلي أو ذكاء اصطناعي لتقييم الخطابة، وأي تكامل OpenAI/Gemini كان مخصصاً لهذه الوظيفة فقط.
- قبل حذف تكامل مشترك، تأكد أنه غير مستخدم لوظائف أخرى. إذا كان مشتركاً، افصل منه تقييم الخطابة الآلي فقط ولا تحذف الخدمة المشتركة.
- لا تعدل migration سبق تطبيقه في قاعدة بيانات Replit. إذا كان رقم `0006` مستخدماً أو المخطط تطور، أنشئ migration جديداً بالترتيب الصحيح يحقق النتيجة نفسها، مع المحافظة على البيانات الموجودة.
- لا تغيّر أسرار البيئة أو تطبعها. لا تربط التطبيق إلزامياً بقاعدة بيانات Replit؛ النشر المستهدف يجب أن يبقى قابلاً للعمل على Vercel مع `DATABASE_URL` خارجية مثل Neon.
- لا تستخدم force-push، ولا ترفع مباشرة فوق `main` قبل نجاح الاختبارات ومراجعة الفرق. ارفع فرع المصالحة إلى GitHub ثم افتح PR.

## ملخص ما نفذه Codex فعلياً

فرع Codex يحتوي ثمانية commits مرجعية:

1. `92caebe` — إصلاح TypeScript وإعادة نجاح typecheck وbuild.
2. `e792e5a` — تحسين تجربة الإطلاق، الدورات الوجاهية، التقييم البشري، وأكواد الخصم.
3. `00d6805` — توافق استيراد `pino-http` مع Vercel.
4. `c98c4a4` — معالجة إخراج TypeScript في بناء Vercel مع بقاء typecheck الصريح صارماً.
5. `334f911` — تثبيت typing لاستجابة fetch الخاصة بواتساب.
6. `a55f2b2` — تصحيح typing لعميل Stripe.
7. `2e7743d` — استيرادات ESM صريحة لخادم API.
8. `65c3c39` — تجميع اعتمادات API workspace بصورة ذاتية لتعمل داخل Vercel Lambda.

لا تنقل commits كما هي؛ استخدمها كمراجع لفهم الحل فقط.

## المتطلبات المطلوب دمجها

### 1. الاستقرار التقني أولاً

قارن إصلاحات commit `92caebe` مع نسخة Replit، وطبّق فقط ما لا يزال ضرورياً، بحيث تنجح من بيئة نظيفة:

```bash
pnpm install
pnpm typecheck
pnpm build
```

الإصلاحات المرجعية شملت:

- إزالة/حل التصديرات المكررة في `lib/api-zod`.
- تصحيح typing الخاص بـ`req.user.id` ومسارات API المتعددة، ومنها طلبات الكراسات.
- تصحيح مفاتيح البيانات والترجمة مثل `fr` و`priceLabel` وتوافقها مع الأنواع.
- تصحيح typing لمخرجات قاعدة البيانات، Stripe، fetch/WhatsApp، وبعض صفحات المدرب والإدارة.
- مزامنة workspace والـlockfile عند الحاجة.

إذا كانت نسخة Replit قد حلت هذه المشكلات بطريقة أحدث، لا تستبدل حلها.

### 2. حذف الاختبار السريع

- احذف فقرة «اختبار سريع — ما البرنامج المناسب لك؟» من الصفحة الرئيسية.
- أزل import والاستدعاء والمسارات والكود الميت المتعلق بها.
- إذا كان الملف أو المكوّن مستخدماً حصراً لهذه الفقرة، يمكن حذفه.
- لا تستبدله باختبار آخر أو recommendation wizard.

### 3. إصلاح بطل الصفحة الرئيسية على الجوال

- يجب ألا تكون واجهة الـhero فارغة على الهاتف.
- السبب في النسخة القديمة كان إخفاء الجزء البصري كله عبر `hidden lg:block`.
- اعرض الصورة/العنصر الرئيسي على الجوال مع aspect ratio وحواف ومسافات مناسبة، ويمكن إبقاء بطاقة الاقتباس الثانوية مخفية على الشاشات الصغيرة إذا كان ذلك أفضل بصرياً.
- افحص فعلياً على مقاسات 320 و375 و390 و768 بكسل، RTL وLTR.

### 4. التفريق بين «الدورات» و«الدورات الوجاهية القادمة»

هذه قاعدة محتوى ومنتج ثابتة:

- قسم **الدورات** يعرض دورات مسجّلة، أو مباشرة عبر Zoom، أو مزيجاً بينهما.
- السعر الظاهر حالياً إن كان سعراً للمسجل يجب تسميته بوضوح «سعر الدورة المسجّلة»، ولا يوحي بأنه سعر كل الصيغ.
- استخدم تسميات مفهومة مثل: «مسجّلة»، «مباشر عبر Zoom»، «مسجّل + Zoom».
- قسم **الفعاليات القادمة** يجب تغيير معناه وعنوانه إلى **الدورات الوجاهية القادمة**، مع نص مساعد يوضح أنها مواعيد التدريب الحضوري.
- لا تعرض في هذا القسم فعاليات عامة أو دورات أونلاين، ولا تعرض موعداً انتهى.
- حافظ على أي CMS أحدث في Replit؛ حدّث القيم الافتراضية والترجمات من دون كسر المحتوى الذي حرره المدير.

معايير بطاقة الدورة الوجاهية:

- اسم الدورة/المسار.
- الجهة أو المكان.
- تاريخ البداية والنهاية.
- الأيام والوقت.
- الموقع/الدولة.
- المقاعد المتبقية إن كانت متوفرة.
- زر «سجّل في هذه الدورة» داخل الموقع نفسه، بلا رابط تسجيل خارجي.

عند الضغط على التسجيل:

- افتح نموذجاً داخلياً مناسباً لتصميم نسخة Replit.
- اجمع الاسم، البريد، الهاتف/واتساب، وملاحظة اختيارية.
- أرسل مع الطلب معرف الموعد والدورة، التاريخ، المكان، ونوع الطلب `group-inperson` أو ما يقابله في نموذج بيانات Replit.
- خزّن الطلب في نظام التسجيل/الـCRM الموجود، ولا تبنِ CRM جديداً.
- اعرض نجاحاً واضحاً بأن الطلب استُلم مبدئياً وأن الفريق سيتواصل لتأكيد المقعد والدفع.
- لا يتم الدفع فوراً في هذا النموذج ما لم تكن نسخة Replit قد أنشأت تدفقاً داخلياً أفضل ومتكاملاً؛ في هذه الحالة حافظ عليه مع بقاء التسجيل داخل الموقع.

المرجع في فرع Codex هو المكوّن `upcoming-course-registration.tsx` وتعديلات `home.tsx` و`translations.ts` وملفات CMS، لكن لا تنسخها حرفياً إذا كانت بنية Replit أحدث.

### 5. «قيّم خطابك»: طلب تقييم بشري فقط

المطلوب النهائي، وهو أهم من أي تنفيذ قديم:

- لا يوجد تحليل فيديو آلي، ولا transcription أو scoring بواسطة OpenAI أو Gemini، ولا وعود بتقييم بالذكاء الاصطناعي.
- لا تقدم هذه كميزة AI في الموقع أو لوحة الطالب أو لوحة المدرب.
- الواجهة العامة الوحيدة تكون في الصفحة الرئيسية بصيغة: «أرسل رابط فيديو لخطابك، وسيطّلع عليه فريق بكلمة ويرسل لك تقييماً أولياً».
- النموذج يجمع رابط فيديو HTTP/HTTPS، الاسم، البريد، ورقم واتساب، مع تحقق ومدخلات محدودة الطول وrate limiting.
- يسجل الطلب كطلب بشري/lead في الـCRM الموجود.
- مسموح فقط بواجهة داخلية إدارية بسيطة لاستلام الطلبات البشرية ومتابعتها إذا كانت ضرورية للعمل؛ لا تنشئ تجربة تقييم آلي أو قسماً تسويقياً مستقلاً للطالب.
- افحص أي صفحات أو tabs حالية تعرض `rubricScores` أو `overallScore` أو تقريراً آلياً للطالب. لا تنقلها من الفرع القديم لمجرد وجودها. اجعل قرار المالك أعلاه هو المرجع.
- أزل تكاملات OpenAI/Gemini الخاصة حصراً بتقييم الخطابة. إذا كانت نسخة Replit تستخدم الذكاء الاصطناعي لغرض آخر معتمد، حافظ عليه وافصل المسارات.

فرع Codex حذف `openaiService.ts` و`geminiService.ts` القديمين المخصصين لهذه الفكرة، وبسّط نموذج الصفحة الرئيسية ومسار `POST /speech-evaluation` ليحفظ الطلب ويضيفه إلى CRM من دون تحليل آلي. لكنه ما زال يحتوي بعض البنية التاريخية للتقارير؛ لا تعتبر هذه البنية إلزامية، وطبّق قرار المالك أعلاه.

### 6. أكواد الخصم في الدفع وإدارتها من لوحة الإدارة

أضف الميزة فقط إذا لم تكن نسخة Replit تحتوي بديلاً مكتملاً، أو أكمل النواقص في البديل الموجود.

المطلوب في لوحة الإدارة:

- صفحة «أكواد الخصم» للمدير فقط.
- إنشاء كود بحروف إنجليزية/أرقام و`_` أو `-`، مع حفظه uppercase ومنع التكرار.
- خصم من نوع نسبة مئوية أو مبلغ ثابت.
- تطبيق على كل الدورات أو دورة محددة.
- تاريخ بداية اختياري، تاريخ انتهاء اختياري، وحد استخدام اختياري.
- عرض عدد مرات الاستخدام، وتفعيل/إيقاف الكود.
- تسجيل الإنشاء والتعديل في سجل التدقيق الموجود.

المطلوب في صفحة الدفع:

- خانة «هل لديك كود خصم؟» وزر تطبيق، مع رسائل عربية/إنجليزية واضحة.
- إظهار السعر الأصلي مشطوباً، مبلغ التوفير، والسعر النهائي.
- الخادم هو مصدر الحقيقة دائماً: يعيد احتساب سعر الدورة والخصم عند إنشاء الطلب، ولا يثق بإجمالي الواجهة.
- رفض الكود المتوقف، غير الصالح زمنياً، الخاص بدورة أخرى، أو الذي بلغ حد الاستخدام.
- منع تجاوز حد الاستخدام عند طلبات متزامنة بعملية transaction/claim ذرية.
- إذا فشل إنشاء الطلب أو بوابة الدفع بعد حجز الاستخدام، أعد عداد الاستخدام بصورة آمنة.
- إذا أصبح السعر صفراً، سجّل الطلب كمدفوع وأنشئ التسجيل مباشرة من دون استدعاء بوابة الدفع.
- خزّن مع الطلب السعر الأصلي، مبلغ الخصم، معرف الكود، نص الكود، والسعر النهائي، وأظهرها في إدارة الطلبات.
- ضع rate limit على التحقق من الأكواد وعلى إنشاء الطلبات.

المرجع البنيوي في فرع Codex:

- جدول `discount_codes` يحوي: `code`, `discount_type`, `discount_value`, `course_id`, `is_active`, `starts_at`, `expires_at`, `max_uses`, `used_count`, `created_by_id`, timestamps.
- حقول الطلب: `original_amount`, `discount_amount`, `discount_code_id`, `discount_code`، بينما `amount` هو النهائي.
- endpoints مرجعية: `POST /discount-codes/validate` و`GET/POST /admin/discount-codes` و`PATCH /admin/discount-codes/:id`.
- migration المرجعي هو `lib/db/drizzle/0006_discount_codes.sql`؛ لا تستخدم رقمه إذا كان تسلسل Replit تجاوزه.

إذا كان لدى Replit نظام coupons أفضل، احتفظ به وحقق معايير القبول أعلاه بدلاً من إنشاء نظام موازٍ.

### 7. صفحة 404 عربية ولائقة

- لا تعرض رسالة المطور الإنجليزية مثل “Did you forget to add the page to the router?”.
- استخدم صفحة 404 عربية/إنجليزية بحسب لغة الموقع، متوافقة مع RTL/LTR وبداخل التصميم العام للموقع.
- وفر زرين واضحين: الصفحة الرئيسية والعودة للخلف.
- حافظ على header/footer أو shell الأحدث في Replit.

### 8. توافق Vercel — مشروط ببنية Replit الحالية

فرع Codex عالج سلسلة مشكلات نشر API على Vercel:

- import متوافق لـ`pino-http`.
- typing آمن لعميل Stripe وfetch/WhatsApp.
- relative ESM imports بامتداد `.js` في خادم API عند الحاجة.
- فصل Express application في `src/application.ts` عن مشغّل الخادم المحلي.
- إنشاء bundle مستقل `dist/app-runtime.mjs` حتى تدخل اعتمادات workspace داخل Lambda ولا تضيع بسبب pnpm symlinks.
- الإبقاء على typecheck صارم كأمر مستقل، مع معالجة خاصة لبناء Vercel traced TypeScript.

طبّق هذه الحلول فقط بقدر ما يناسب بنية Replit الأحدث. إذا كانت قد غيّرت طريقة البناء أو أصلحت المشكلة، لا ترجعها إلى بنية أقدم. معيار القبول هو:

- التشغيل المحلي في Replit يبقى ناجحاً.
- `pnpm typecheck` و`pnpm build` ينجحان.
- Vercel يبني API من دون أخطاء ESM أو modules مفقودة.
- التشغيل على Vercel يعتمد على `DATABASE_URL` الخارجية وبيئة صحيحة، لا على قاعدة Replit أو متغيرات Replit وحدها.
- لا تعتبر عدم وجود `DATABASE_URL` عيب كود؛ وثّق المتغيرات المطلوبة من دون كشف قيمها.

## ميزات موجودة يجب الحفاظ عليها وعدم إعادة بنائها

افحصها فقط للتأكد أنها لم تتأثر بالدمج:

- CRM في `/admin`: العملاء/leads، Pipeline، automations، message templates، المهام، وسجل التدقيق.
- استرداد كلمة المرور عبر forgot/reset password، الرموز أحادية الاستخدام وصلاحيتها، وإبطال الجلسات بعد التغيير.
- تذكير تأكيد البريد وزر إعادة الإرسال في لوحة الطالب.
- Google OAuth المشروط بمتغيرات البيئة وإظهار الزر فقط عند التفعيل.
- التسجيل المبسط بالبريد وكلمة المرور.
- أي تحسينات أحدث في صفحات الطالب والمدرب والإدارة، المحتوى، التصميم، الأمن، الأداء، الوصول، الاختبارات، أو قاعدة البيانات داخل نسخة Replit.

لا تستبدل هذه الميزات بنسخة فرع Codex الأقدم. إذا لمس diff ملفاً تابعاً لها، انقل فقط السطور اللازمة للـtyping أو أكواد الخصم أو المتطلبات السابقة.

## أشياء غير مطلوبة الآن

- لا تبنِ Open Badges 3.0.
- لا تبنِ LTI 1.3.
- لا تبدأ OWASP ASVS Level 2 كمشروع مستقل.
- لا تضف Canary deployment.
- لا تضف اختباراً سريعاً أو recommender.
- لا تضف تقييم خطابة بالذكاء الاصطناعي.
- لا تربط قاعدة البيانات بقاعدة Replit لمجرد أن العمل يتم داخل Replit.

## ترتيب التنفيذ المطلوب

1. أنشئ checkpoint/backup كامل لنسخة Replit الحالية في branch وcommit منفصلين.
2. اجلب `origin/codex/launch-wave` للمقارنة.
3. أخرج مصفوفة التقاطع لكل بند أعلاه، مع الملفات المقابلة في Replit وفي فرع Codex، ولا تبدأ التعديل قبلها.
4. أصلح typecheck/build فقط حيث ما زالت الأخطاء قائمة.
5. نفذ حذف الاختبار، hero الجوال، 404، وتصنيف الدورات/الوجاهي.
6. نفذ التسجيل الداخلي للدورات الوجاهية مع دمجه في CRM الحالي.
7. طبق التقييم البشري فقط وأزل أي مسار AI مخصص له.
8. نفذ/ادمج أكواد الخصم end-to-end مع migration آمن.
9. طبق توافق Vercel الضروري فقط.
10. شغّل الاختبارات والفحص البصري والوظيفي.
11. راجع `git diff` للتأكد من عدم رجوع أي تحسين خاص بنسخة Replit.
12. ارفع branch المصالحة إلى GitHub وافتح PR إلى `main`. لا force-push ولا merge تلقائي إلى `main`.

## اختبارات القبول قبل الرفع

- تثبيت نظيف ثم `pnpm typecheck` ثم `pnpm build`، وكلها ناجحة.
- الاختبارات الآلية الحالية ناجحة، مع تحديث اختبارات رحلة المتعلم لما تغير فعلاً.
- الصفحة الرئيسية تعمل على الهاتف ولا يوجد فراغ في hero.
- لا يظهر الاختبار السريع في أي لغة أو مسار.
- قسم الدورات يشرح Recorded/Zoom/Blended، وقسم مستقل للدورات الوجاهية القادمة فقط.
- المواعيد المنتهية غير ظاهرة.
- التسجيل الوجاهي يتم داخل الموقع ويصل إلى النظام الداخلي/CRM.
- لا توجد أي دعوى أو عملية تقييم آلي للخطابة، والنموذج العام يطلب رابط الفيديو للمراجعة البشرية.
- المدير يستطيع إنشاء كود خصم، تقييده، إيقافه، وتتبّع استخدامه.
- checkout يعيد حساب الخصم على الخادم ويخزن تفاصيله الصحيحة.
- صفحة 404 معرّبة ولا تسرب رسالة داخلية.
- لا يوجد regression في CRM والمصادقة واسترداد كلمة المرور وGoogle OAuth ولوحات المستخدمين.
- build الخاص بـVercel ناجح، مع توثيق متغيرات البيئة المطلوبة.

## التقرير المطلوب منك عند الانتهاء

لا تقل فقط «تم». أرسل تقريراً يتضمن:

1. مصفوفة التقاطع: منجز في Replit / جزئي / جديد / متعارض.
2. ما أخذته من فرع Codex تحديداً، وما لم تأخذه ولماذا.
3. تحسينات Replit الأحدث التي حافظت عليها.
4. كل الملفات والمigrations التي عدلتها.
5. نتائج أوامر install/typecheck/build/tests.
6. فحص الجوال وRTL/LTR والتدفقات الأساسية.
7. أي متغيرات بيئة أو خطوات migration مطلوبة، من دون عرض أسرار.
8. رابط فرع GitHub والـPR والـcommit النهائي.
9. أي قرار ما زال يحتاج موافقة المالك؛ لا تفترضه ولا تحذف شيئاً إضافياً.

---

## ملاحظة حماية أخيرة

فرع `codex/launch-wave` مفيد كمرجع للتغييرات، لكنه مبني على GitHub قديم. **ممنوع اعتماده كنسخة نهائية أو دمجه كاملاً فوق Replit.** النتيجة الصحيحة هي: نسخة Replit الأحدث + المتطلبات الجديدة أعلاه + الحفاظ على كل تحسين لا يتعارض معها.
